FROM python:3.12-slim

WORKDIR /app

RUN pip install uv

COPY pyproject.toml uv.lock ./
RUN uv sync --group frontend

COPY frontend/ ./frontend/

EXPOSE 8501

CMD ["uv", "run", "--group", "frontend", "streamlit", "run", "frontend/app.py", "--server.address", "0.0.0.0", "--server.port", "8501"]
