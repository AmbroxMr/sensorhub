apiVersion: v1
kind: Service
metadata:
  name: frontend
  namespace: sensorhub
spec:
  type: NodePort
  selector:
    app: frontend
  ports:
    - name: http
      port: 8501
      targetPort: 8501
      nodePort: 30801
